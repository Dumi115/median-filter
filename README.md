The console input for this program shoud be:
  -"merge" or "bubble" for the type of sort you want to use;
  -a number for the dimension of the pgm image;
  -the name of the input file;
  -the name of the output file.
An example of a console imput can be: "merge 256 input.pgm output.pgm"
